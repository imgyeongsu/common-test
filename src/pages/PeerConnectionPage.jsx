/**
 * Peer Connection Page
 *
 * LocalPeerConnection feature를 사용하는 페이지
 */

import { LocalPeerConnection } from '@/features/webrtc-learning';

function PeerConnectionPage() {
  return <LocalPeerConnection />;
}

export default PeerConnectionPage;
